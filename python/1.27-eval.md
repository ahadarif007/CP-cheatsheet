## eval

`eval()` evaluates a string as a Python expression and returns the result. Useful for simple calculations but DANGEROUS with untrusted input (security risk!):

```python
x = 1
print(eval('x + 1'))   # 2 (evaluates the expression in current scope)
```

