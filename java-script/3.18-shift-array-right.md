# Shift Array Right

```js
function rotate(a, k) {
  const n = a.length;
  k %= n;
  rev(a, 0, n - 1);
  rev(a, 0, k - 1);
  rev(a, k, n - 1);
}
function rev(a, l, r) {
  while (l < r) {
    [a[l], a[r]] = [a[r], a[l]];
    l++;
    r--;
  }
}
```
