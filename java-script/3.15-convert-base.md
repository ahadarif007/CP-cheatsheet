# Convert Base

```js
function toHex(num) {
  if (num === 0) return '0';
  if (num < 0) num = 2 ** 32 + num;
  const idx = '0123456789abcdef';
  let s = '';
  while (num > 0) {
    const d = num % 16;
    s = idx[d] + s;
    num = Math.floor(num / 16);
  }
  return s;
}
```
