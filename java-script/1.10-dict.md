# Dict

```js
const d = new Map();
d.set('key', 1);
const v = d.get('key') ?? 0;
for (const k of d.keys()) {
}
for (const [k, val] of d.entries()) {
}
if (!d.has('k')) d.set('k', 0);
d.delete('k');
```
