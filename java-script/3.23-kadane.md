# Kadane's Algorithm

```js
function maxSubArray(a) {
  let best = a[0];
  for (let i = 1; i < a.length; i++) {
    if (a[i - 1] > 0) a[i] += a[i - 1];
    best = Math.max(best, a[i]);
  }
  return best;
}
```
