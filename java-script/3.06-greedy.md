# Greedy

Increasing triplet

```js
function increasingTriplet(nums) {
  let a = Infinity,
    b = Infinity;
  for (const x of nums) {
    if (x <= a) a = x;
    else if (x <= b) b = x;
    else return true;
  }
  return false;
}
```
